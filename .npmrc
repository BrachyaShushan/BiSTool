# Configure npm to handle optional dependencies better
optional=false
legacy-peer-deps=true
omit=optional

# Disable platform-specific binaries that cause issues on Netlify
rollup-optional-dependencies=false 